<% if @feed_items.any? %>
  <ol class="microposts">
    <%= render @feed_items %><!-- Rails knows that every @feed_items belongs to Micropost and calls partial of microposts => app/views/microposts/_micropost.html.erb -->
  </ol>
  <%= will_paginate @feed_items %>
<% end %>